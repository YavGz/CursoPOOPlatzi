class Person: 
  name = ""; 
  def walk():
    #logica de la function#
  
  # el atributo se define solo con el nombre dela variable# 
  #"def" es la palabra reservada para la funcion y se cierra con ":" asi se define un comportamiento en python. Lo siguiente que resibe, la logica del comportamiendo de la funcion debe de estar en otra linea con u tab (una linea identada)
